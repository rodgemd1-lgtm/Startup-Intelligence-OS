# Codex Brief — Decision & Capability OS

Build this repo into a **Decision & Capability OS** with:
- Jake as the default front door
- Susan as the capability foundry
- terminal as the primary execution surface
- interface as the control plane
- workspace-native structured state
- explicit decisions, capabilities, projects, companies, artifacts, and runs

Start with the plan files under `docs/plans/decision-capability-os/`.
Do not rewrite the existing Susan system blindly.
Work in staged, reviewable increments.
