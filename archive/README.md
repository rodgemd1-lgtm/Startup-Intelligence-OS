# Archive

This area stores legacy/reference material that no longer defines the active root operating model.

- `resource-hub/` contains the original founder resource hub category directories moved from repo root.

These materials are preserved for reference and can be selectively promoted back if needed.
